
// // 获取计算属性
// function getstyle(ele, attr) {
//     return (ele.currentStyle ? ele.currentStyle[attr] : window.getComputedStyle(ele)[attr]);
// }

import { getstyle } from "./methods.js";

const utils = {
    getstyle
}
export default utils






